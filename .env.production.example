# Optional overrides. Leave unset so the built app uses same-origin
# `/api/stream/*` (Cloudflare Pages `_worker.js` or local Express).
# To force the dedicated stream Worker instead, set:
# VITE_NAZDRAVE_STREAM_URL=https://live.folkradionazdrave.com/api/stream/nazdrave
# VITE_GOLD_STREAM_URL=https://live.folkradionazdrave.com/api/stream/gold
